export class Member {
  constructor (public name: string, public age: number, public gender: string, public bio: string, public interest: string, public image: string, public favoritegenre: string, public currentsong: string) { }
}
